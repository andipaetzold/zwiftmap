runtime: nodejs16

instance_class: F1

inbound_services:
  - warmup

automatic_scaling:
  max_instances: 1

env_variables:
  BACKEND_URL: https://api2.zwiftmap.com
  FRONTEND_URL: https://zwiftmap.com

  STRAVA_CLIENT_ID: 66426

  PORT: 8080
