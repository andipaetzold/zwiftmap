{
  "name": "zwiftmap",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "browser-extension",
    "frontend"
  ],
  "engines": {
    "node": "16.15.0"
  },
  "scripts": {
    "start": "turbo run start",
    "build": "turbo run build",
    "heroku-postbuild": "turbo run build -- --filter=@zwiftmap/backend",
    "deploy": "turbo run deploy",
    "test": "CI=true turbo run test"
  },
  "devDependencies": {
    "turbo": "1.2.9"
  }
}
