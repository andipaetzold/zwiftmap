{
  "name": "zwiftmap",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "browser-extension",
    "frontend"
  ],
  "scripts": {
    "start": "turbo run start",
    "build": "turbo run build",
    "deploy": "turbo run deploy",
    "test": "CI=true turbo run test"
  },
  "devDependencies": {
    "turbo": "1.2.9"
  }
}
